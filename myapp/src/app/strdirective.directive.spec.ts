import { StrdirectiveDirective } from './strdirective.directive';

describe('StrdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StrdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
